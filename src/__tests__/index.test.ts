import { addDevtools, makeLocalGetters } from "../index";

test("test addDevtools() exists", () => {
  expect(addDevtools).toBeTruthy();
});

test("test makeLocalGetters() exists", () => {
  expect(makeLocalGetters).toBeTruthy();
});
